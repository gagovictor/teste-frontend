import React, { Component } from 'react';
import './index.scss';

class contactModal extends Component {
  render() {
    return (
      <div id="contact">
      </div>
    );
  }
}

export default contactModal;
